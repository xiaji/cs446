<%= form_for(@considering) do |f| %>
  <% if @considering.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@considering.errors.count, "error") %> prohibited this considering from being saved:</h2>

      <ul>
      <% @considering.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
