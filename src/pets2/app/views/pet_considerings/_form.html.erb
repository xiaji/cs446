<%= form_for(@pet_considering) do |f| %>
  <% if @pet_considering.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pet_considering.errors.count, "error") %> prohibited this pet_considering from being saved:</h2>

      <ul>
      <% @pet_considering.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.text_field :pet_id %>
  </div>
  <div class="field">
    <%= f.label :considering_id %><br>
    <%= f.text_field :considering_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
